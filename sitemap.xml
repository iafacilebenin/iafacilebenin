iafacilebenin/                  ← your repo root
├── index.html                  ← Homepage (your existing empire landing)
├── sitemap.xml                 ← NEW (the file I gave you)
├── robots.txt                  ← NEW (rename DOC_2026-03-25_robots.txt → robots.txt)
├── favicon.svg                 ← Already there ✓
├── og-image.png                ← Already there ✓
│
├── sites/
│   └── index.html              ← The pricing page I just built (you're doing this now ✓)
│
├── prompts/
│   └── index.html              ← Prompt pack page (build later)
│
├── bot/
│   └── index.html              ← Telegram bot page (build later)
│
├── maitreetude/
│   └── index.html              ← Exam prep page (build later)
│
├── zemivivant/
│   └── index.html              ← Road safety B2G page (build later)
│
├── lrdy/
│   └── index.html              ← Music page (build later)
│
├── fon/
│   └── index.html              ← Corpus page (build later)
│
└── about/
    └── index.html              ← About page (build later)
